describe('npm-shrinkwrap.json', function () {
  it('should not include fsevents (a macOS-only package)', function (done) {
    // expect(shrinkwrap.dependencies.fsevents).toBe(undefined)
    done()
  })
})
